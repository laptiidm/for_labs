# **Лабораторна робота №4**

## **1. Тема роботи**

Керування яскравістю та режимами роботи світлодіодів на Arduino за допомогою PWM.

---

## **2. Конкретні завдання**

### **Завдання 1**

Перепідключити світлодіод з порту **D9** на **D11**.
Змінити програму так, щоб схема знову працювала з тим самим режимом блимання.

### **Завдання 2**

Змінити програму так, щоб протягом **1 секунди** на світлодіод подавались послідовно усереднені значення напруги:
**0 В, 1 В, 2 В, 3 В, 4 В, 5 В**
(через PWM).

### **Завдання 3**

Додати другий світлодіод (через окремий резистор 220 Ом) на пін **D3**.
Створити програму, де **два світлодіоди працюють у протифазі**:

* коли перший світить, другий вимкнений
* коли другий світить, перший вимкнений

---

## **3. Код програм**

### **3.1. Код для завдання 1**

Перенесення LED з порту 9 → 11.

```cpp
const int ledPin = 11;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  digitalWrite(ledPin, HIGH);
  delay(100);
  digitalWrite(ledPin, LOW);
  delay(900);
}
```

---

### **3.2. Код для завдання 2**

Послідовна подача 0…5 В протягом 1 секунди.

```cpp
const int ledPin = 11;

void setup() {
  pinMode(ledPin, OUTPUT);
}

void loop() {
  const int levels[6] = {0, 51, 102, 153, 204, 255}; 
  const int stepDelay = 1000 / 6; // ≈167 мс

  for (int i = 0; i < 6; i++) {
    analogWrite(ledPin, levels[i]);
    delay(stepDelay);
  }

  delay(500);
}
```

---

### **3.3. Код для завдання 3**

Два світлодіоди у протифазі (пін 11 і 3).

```cpp
const int led1Pin = 11; // червоний
const int led2Pin = 3;  // зелений (новий світлодіод)

void setup() {
  pinMode(led1Pin, OUTPUT);
  pinMode(led2Pin, OUTPUT);
}

void loop() {
  analogWrite(led1Pin, 0);    // червоний вимкнено
  analogWrite(led2Pin, 255);  // зелений світить
  delay(500);

  analogWrite(led1Pin, 255);  // червоний світить
  analogWrite(led2Pin, 0);    // зелений вимкнено
  delay(500);
}
```

---

## **4. Схема підключення (Wokwi)**

Проєкт містить:

* Arduino Uno
* Breadboard
* Світлодіод 1: пін **11 → резистор → анод LED → катод → GND**
* Світлодіод 2: пін **3 → резистор → анод LED → катод → GND**

Симуляція працює коректно, обидва LED працюють у відповідності до завдань.

---

## **5. Посилання на онлайн-симуляцію**

🔗 **Wokwi проєкт:**
[https://wokwi.com/projects/448326442122963969](https://wokwi.com/projects/448326442122963969)

---

