## 📋 Звіт до лабораторної роботи №7 «Світломузика»

-----

### 1\. Тема роботи

Програмування трьох світлодіодів для реалізації **накопичувальної світлової послідовності**: почергове вмикання (один, потім другий, потім третій) та одночасне вимикання всіх.

### 2\. Мета роботи

Набути практичних навичок роботи з цифровими виходами Arduino та функцією `delay()`, реалізувавши складний логічний цикл керування трьома світлодіодами: накопичувальне вмикання та синхронне вимкнення.

-----

### 3\. Необхідне обладнання

| Обладнання | Кількість | Призначення |
| :--- | :--- | :--- |
| Електронна плата **Arduino Uno R3** | 1 | Мікроконтролер |
| Світлодіоди (LED) | 3 | Візуалізація виходу |
| Резистори **220 Ом** | 3 | Обмеження струму для захисту LED |
| **Макетна дошка** (Breadboard) | 1 | З'єднання компонентів |

-----

### 4\. Принципова схема підключення

Світлодіоди підключені до цифрових пінів Arduino через струмообмежувальні резистори **220 Ом**. Для спрощення схеми та реалістичності, спільна шина **GND** (земля) макетної дошки використовується для підключення катодів усіх трьох світлодіодів.

| Світлодіод | Пін Arduino | Функція |
| :--- | :--- | :--- |
| **LED1** | **D13** | Перший у послідовності |
| **LED2** | **D12** | Другий у послідовності |
| **LED3** | **D11** | Третій у послідовності |

-----

### 5\. Код програми (sketch.ino)

Використовується кумулятивна логіка в `loop()`, що забезпечує **почергове вмикання** (накопичення) та **одночасне вимкнення** (скидання).

```cpp
#define LED1_PIN 13
#define LED2_PIN 12
#define LED3_PIN 11

#define ON_DELAY_MS 400
#define OFF_DELAY_MS 500

void setup() {
  pinMode(LED1_PIN, OUTPUT);
  pinMode(LED2_PIN, OUTPUT);
  pinMode(LED3_PIN, OUTPUT);
}

void all_off() {
  digitalWrite(LED1_PIN, LOW);
  digitalWrite(LED2_PIN, LOW);
  digitalWrite(LED3_PIN, LOW);
  delay(OFF_DELAY_MS);
}

void loop() {
  digitalWrite(LED1_PIN, HIGH);
  delay(ON_DELAY_MS);
  

  digitalWrite(LED2_PIN, HIGH);
  delay(ON_DELAY_MS);
  

  digitalWrite(LED3_PIN, HIGH);
  delay(ON_DELAY_MS);
  
  all_off();
}
```

-----

### 6\. Опис та пояснення процесів

#### А. Процеси, що спостерігаються

Послідовність світлодіодів створює ефект **накопичення**:

1.  **LED1** вмикається і горить **400 мс**.
2.  **LED2** вмикається (LED1 продовжує горіти). Пауза **400 мс**.
3.  **LED3** вмикається (Всі 3 LED горять). Пауза **500 мс**.
4.  **ВСІ ТРИ LED** вимикаються **одночасно** (синхронне вимкнення) завдяки функції `all_off()`.
5.  Після паузи в 500 мс цикл повторюється.

#### Б. Фізичне пояснення та програмні команди

  * **digitalWrite(PIN, HIGH):** Встановлює напругу **5 В** на цифровому піні. Оскільки світлодіод із резистором підключений між піном та землею (GND), ланцюг замикається, і світлодіод світиться.
  * **Кумулятивне вмикання:** Реалізовано через те, що **`digitalWrite(LEDn_PIN, HIGH)`** не вимикає попередні світлодіоди. Після увімкнення LED1, він залишається у стані HIGH, поки явно не буде встановлено у LOW.
  * **Синхронне вимкнення (`all_off()`):** Забезпечує виконання команди `digitalWrite(LEDx_PIN, LOW)` для всіх трьох пінів послідовно, але за дуже короткий проміжок часу, що для людського ока виглядає як **одночасне вимкнення** (разом).
  * **Роль `delay()`:** Контролює швидкість світлової послідовності, забезпечуючи, щоб кожен етап — вмикання, накопичення та пауза — був помітним.


